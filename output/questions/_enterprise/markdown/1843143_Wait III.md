# Wait III

## Metadata

- **ID:** 1843143
- **Type:** mcq
- **Difficulty:** 1
- **Points:** 5
- **Duration:** N/A minutes
- **Tags:** Selenium, Medium
- **Skills:** Selenium (Intermediate)

## Summary

This multiple choice question evaluates implicit wait, explicit wait, and exception handling concepts, ideal for mid-level roles. The problem requires calculating the total wait time of a Selenium script based on various wait conditions.

## Problem Statement

This pseudo code uses implicit wait and explicit wait. How much time will the script wait?

 

`driver.implicitly_wait(10)
try:
    driver.find_element(By.XPATH, '//fakepath').send_keys('test')

except TimeoutException:
    try:
        driver.execute_script('alert("TimeoutException");')
        WebDriverWait(driver, 15).until(EC.alert_is_present())
        alert = driver.switch_to.alert
        alert.accept()
        try:
            driver.find_element(By.XPATH, "//fakepath")
        except:
            print("No such element")

    except UnexpectedAlertPresentException:
        WebDriverWait(driver, 50).until(EC.alert_is_present())
        print("UnexpectedAlertPresentException")

except NoSuchElementException:
    try:
        WebDriverWait(driver, 25).until(
            EC.visibility_of_element_located((By.XPATH, "//fakepath"))
        )
    except TimeoutException:
        try:
            driver.find_element(By.XPATH, "//fakepath")
        except:
            print("No such element")
    except NoSuchElementException:
        try:
            driver.execute_script('alert("TimeoutException");')
            WebDriverWait(driver, 5).until(EC.alert_is_present())
            alert = driver.switch_to.alert
            alert.accept()
        except:
            print("No alert found")

except ElementNotInteractableException:
    try:
        driver.find_element(By.XPATH, "//fakepath")
    except:
        print("No such element")`
```

## Preview

This pseudo code uses implicit wait and explicit wait. How much time will the sc
